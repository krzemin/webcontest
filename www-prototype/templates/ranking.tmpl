<h2>Ranking</h2>
<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>No</th>
      <th>Name</th>
      {{#each problems}}
      	<th class="hidden-phone">{{this}}</th>
      {{/each}}
      <th>Total</th>
    </tr>
  </thead>
  <tbody>
  	{{#each board}}
  	<tr>
  		<td>{{no}}</td>
  		<td>{{name}}</td>
  		{{#each problems}}
      	<td class="hidden-phone">{{this}}</td>
      {{/each}}
      <td><b>{{score}}</b></td>
  	</tr>
  	{{/each}}
 </tbody>
</table>