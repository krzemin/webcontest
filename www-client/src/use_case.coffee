class UseCase
  constructor: ->
    
  start: =>

  signIn: (email, password) =>
    if email == 'a' and password == 'a'
      @loadContestList

  loadContestList: =>


  
  initCodeView: =>
     
  codeChanged: (newText) =>

   
