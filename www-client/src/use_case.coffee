class UseCase
  constructor: ->
    
  start: =>

  signIn: (credentials) =>
    console.log(credentials)
    if credentials.email == 'a' and credentials.password == 'a'
      @loadContestList()

  loadContestList: =>

  openContest: (id) =>
    
  
  initCodeView: =>
     
  codeChanged: (newText) =>

   
